# password-breach-checker-python
🔐 Python CLI tool that checks if passwords have appeared in known data breaches using the HaveIBeenPwned k-anonymity API — no plaintext password ever leaves your machine.
